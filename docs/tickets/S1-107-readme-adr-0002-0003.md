---
title: "S1-107 README quick start and ADRs 0002 (single table) and 0003 (global name uniqueness)"
labels: type:docs, pillar:operations
milestone: S1 Create project
status: open
---
## Description
Write `README.md`: what the service does, architecture summary (link to `docs/architecture.md`), prerequisites (uv, terraform >= 1.3, aws cli, docker), quick start (install, lint, test), local run (docker compose + `make run-local` + curl examples including the 201/409/400 cases), build (`make build`), deploy (`make tf-bootstrap`, copy `backend.example.hcl` to `backend.hcl`, `make tf-init tf-plan tf-apply`), onboarding a user (`scripts/create_api_key.sh`), smoke test (`make smoke`), API reference table (method, path, auth, responses), project layout, and links to docs/ADRs/tickets.

Write ADRs in `docs/adr/`:
- `0002-single-table-design.md`: why one table with generic PK/SK partitioned by project id, item types, GSI1 for owner listing, how new entities are added, trade-offs versus one table per entity.
- `0003-global-name-uniqueness.md`: why names are globally unique and case-insensitive (future hostnames), the reservation-item + transaction pattern, why not a GSI uniqueness check (race), how deletion will release the reservation (S4-401).

## Acceptance criteria
- Given README, then every command shown exists in the Makefile or scripts and is spelled identically.
- Given the curl examples, then request bodies match `CreateProjectRequest` and the shown responses match the actual problem+json shape from `api/errors.py`.
- Given the ADRs, then each has Status, Context, Decision, Consequences and a date.

## Dependencies
Coordinate with S0-007 only via the README link; do not edit `docs/architecture.md`.

## Definition of Done
Merged; `make lint` unaffected.
